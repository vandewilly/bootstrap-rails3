%ul.nav.pull-right
  - if user_signed_in?
    %li
      = link_to("Dashboard", dashboard_path)
    %li
      = link_to("Sign out", sign_out_path, data: { confirm: "Are you sure?" })
  - else
    %li
      = link_to("Sign in", sign_in_path)
